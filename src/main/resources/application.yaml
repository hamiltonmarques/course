server:
  port: 8082

spring:
  application:
    name: ead-course-service

  datasource:
    url: jdbc:postgresql://localhost:5432/ead-course
    username: postgres
    password: pos123

  jpa:
    hibernate:
      ddl-auto: validate

#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

  flyway:
    enabled: true

  jackson:
    time-zone: America/Sao_Paulo
    deserialization:
      accept-float-as-int: false

app:
  clients:
    auth-user-api:
      label: AUTH-USER-API
      base-url: http://ead-authuser-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost